version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 8
  build:
    commands:
      - echo Entered the build phase...
      - echo Nothing to do yet
artifacts:
  files:
    - index.html
    - favicon.ico
    - styles/main.css
    - images/*
